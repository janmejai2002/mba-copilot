from core.state import AgentState

async def navigator_node(state: AgentState):
    """
    Navigator Agent: Expands concepts and generates UI payloads.
    placeholder for now.
    """
    return {"messages": ["NavigatorAgent: Generating visual payload..."]}
